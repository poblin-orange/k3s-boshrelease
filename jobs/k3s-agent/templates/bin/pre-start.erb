#!/bin/sh
/var/vcap/packages/k3s/k3s check-config

#prepare a persistent directory so /etc/rancher/node paswword file is kept on bosh recreate
mkdir -p /etc/rancher
mkdir -p /var/vcap/store/k3s-node/etc/rancher/node
ln -sf /etc/rancher/node /var/vcap/store/k3s-node/etc/rancher/node 

exit 0
